import React from 'react';
import { render, wait, within } from '@testing-library/react';
import '@testing-library/jest-dom'
import User from '../User';

describe('<User /> ', () => {
  test('renderizar corretamente ', () => {
    //validar se é apresentado o formulário, pelo menos três inputs de texto
  });

  test('espera que adicione um aluno', () =>{
    //fazer os testes para verificar que clicando em Adicionar button
    // irá adicionar um aluno 
  });

  test('espera que aceite um aluno', () => {
    // fazer com que adicione um aluno e realize o aceite na listagem de baixo
  })

})